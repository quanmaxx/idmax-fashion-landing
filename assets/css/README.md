# CSS

"theme.css" chứa phần CSS tùy biến được tách từ file HTML gốc. Tailwind vẫn được nạp qua CDN và tiếp tục xử lý các class bố cục của thiết kế gốc.

Chỉ chỉnh file này khi cần thay màu nền cơ bản, hiệu ứng chữ gradient, kính mờ hoặc lưới nền.
